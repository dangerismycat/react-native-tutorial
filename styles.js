import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainView: {
    alignItems: 'center',
  },
  blink: {
    alignItems: 'center',
  },
  buster: {
    borderRadius: 5,
    flexGrow: 1,
    marginTop: 30,
  },
  greeting: {
    marginTop: 15,
  },
});

export default styles;
